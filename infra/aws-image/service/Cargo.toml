[package]
name = "wasmplane-deployment-sample"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wasmplane-service-sdk = { path = "../../../sdk/rust" }
