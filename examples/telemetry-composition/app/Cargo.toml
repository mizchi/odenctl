[package]
name = "telemetry-composed-app"
version = "0.1.0"
edition = "2024"
[lib]
crate-type = ["cdylib"]
[dependencies]
wit-bindgen = { version = "=0.62.0", features = ["async"] }
wasmplane-service-sdk = { path = "../../../sdk/rust" }
