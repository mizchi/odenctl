[package]
name = "exported-tests"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wasmplane-service-sdk = { path = "../../../sdk/rust" }
wit-bindgen = { version = "=0.62.0", features = ["async"] }
futures-channel = "0.3"

[workspace]
