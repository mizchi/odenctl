app = "wasmplane-runtime"
primary_region = "nrt"

[build]
  dockerfile = "Dockerfile"

[processes]
  app = "node --experimental-strip-types src/runtime/main.ts"

[env]
  RUNTIME_HOST = "0.0.0.0"
  RUNTIME_PORT = "8080"
  RUNTIME_CONCURRENCY = "64"
  RUNTIME_MEMORY_MB = "2048"
  RUNTIME_PUBLIC_URL = "auto"
  RUNTIME_SNAPSHOT_WARMUP = "1"
  RUNTIME_ROUTE_SNAPSHOT_FILE = "/data/route-snapshot.json"
  WASMPLANE_CACHE_DIR = "/data/cache"
  WASMPLANE_ARTIFACT_CACHE_DIR = "/data/artifacts"
  WASMPLANE_KV_STORE_DIR = "/data/kv"
  WASMPLANE_WASIP3_HOST_BIN = "/usr/local/bin/wasmplane-wasip3-host"
  WASMPLANE_WASIP3_HOST_DAEMON = "1"
  WASMPLANE_WASIP3_HOST_DAEMON_PORT = "8790"
  WASMPLANE_WASIP3_HOST_MAX_PREPARED_COMPONENTS = "512"
  WASMPLANE_WASIP3_HOST_MAX_CONCURRENT_INVOCATIONS = "64"
  WASMPLANE_WASIP3_POOLING_TOTAL_COMPONENT_INSTANCES = "64"
  WASMPLANE_WASIP3_POOLING_TOTAL_CORE_INSTANCES = "256"
  WASMPLANE_WASIP3_POOLING_TOTAL_MEMORIES = "64"
  WASMPLANE_WASIP3_POOLING_TOTAL_TABLES = "128"
  WASMPLANE_WASIP3_POOLING_MEMORY_MB = "64"

[mounts]
  source = "wasmplane_runtime_data"
  destination = "/data"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

  [[http_service.checks]]
    grace_period = "10s"
    interval = "30s"
    method = "GET"
    timeout = "5s"
    path = "/__runtime/readyz"

[[vm]]
  cpu_kind = "performance"
  cpus = 1
  memory = "2gb"
